
#No supe como traer el resultado a la consola
#_Consulta="SELECT SUM(costo) FROM contrato con JOIN contrato_persona cp ON (con.idcontrato=cp.idcontrato) JOIN persona per ON (per.idpersona=cp.idpersona) WHERE per.correo =samuel@gmail.com"
